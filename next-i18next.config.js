const config = {
    i18n: {
        locales: ['en', 'ko'],
        defaultLocale: 'en',
        localeDetection: false,
    },
    fallbackLng: ['en', 'ko'],
    defaultNS: 'common',
};

module.exports = config;
